version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "nodejs-app-task"
        LoadBalancerInfo:
          ContainerName: "nodejs-container"
          ContainerPort: 3000
